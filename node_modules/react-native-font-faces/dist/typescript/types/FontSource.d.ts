import { FontFormat } from './FontFormat';
export declare type FontSource = FontSourceLocal | FontSourceUrl;
export declare function local(name: string): FontSourceLocal;
export declare function url(url: string, format: FontFormat): FontSourceUrl;
declare type FontSourceLocal = {
    type: 'local';
    name: string;
};
declare type FontSourceUrl = {
    type: 'url';
    url: string;
    format: FontFormat;
};
export {};
