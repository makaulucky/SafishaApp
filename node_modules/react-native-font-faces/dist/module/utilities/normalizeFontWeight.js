export function normalizeFontWeight(fontWeight) {
  if (fontWeight === 'bold') {
    return 700;
  } else if (fontWeight === 'normal') {
    return 400;
  } else if (typeof fontWeight === 'string') {
    return Number.parseInt(fontWeight);
  } else {
    return fontWeight;
  }
}
//# sourceMappingURL=normalizeFontWeight.js.map