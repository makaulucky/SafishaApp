import { getLocalFontName } from './getLocalFontName';
import { getExpoFontMapEntry } from './getExpoFontMapEntry';
export function getExpoFontMap(fontFaces) {
  const obj = {};

  for (const fontFace of fontFaces) {
    var _getExpoFontMapEntry;

    const fontName = getLocalFontName(fontFace);
    const remoteUrl = (_getExpoFontMapEntry = getExpoFontMapEntry(fontFace)) === null || _getExpoFontMapEntry === void 0 ? void 0 : _getExpoFontMapEntry.url;

    if (remoteUrl) {
      obj[fontName] = remoteUrl;
    }
  }

  return obj;
}
//# sourceMappingURL=getExpoFontMap.js.map