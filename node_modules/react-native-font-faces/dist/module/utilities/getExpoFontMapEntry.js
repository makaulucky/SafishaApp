import { explodeToArray } from './explodeToArray';
export function getExpoFontMapEntry(fontFace) {
  for (const source of explodeToArray(fontFace.src)) {
    if (source.type === 'url') {
      const {
        url,
        format
      } = source;
      return {
        url,
        format
      };
    }
  }
}
//# sourceMappingURL=getExpoFontMapEntry.js.map