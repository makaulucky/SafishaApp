"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getDynamicFontListEntry = getDynamicFontListEntry;

var _explodeToArray = require("./explodeToArray");

var _getLocalFontName = require("./getLocalFontName");

function getDynamicFontListEntry(fontFace) {
  for (const source of (0, _explodeToArray.explodeToArray)(fontFace.src)) {
    if (source.type === 'url') {
      const {
        url,
        format
      } = source;
      const name = (0, _getLocalFontName.getLocalFontName)(fontFace);
      const data = url;
      const type = format === 'opentype' ? 'otf' : format === 'truetype' ? 'ttf' : undefined;
      return {
        name,
        data,
        type
      };
    }
  }
}
//# sourceMappingURL=getDynamicFontListEntry.js.map