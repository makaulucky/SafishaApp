"use strict";

var _normalizeFontWeight = require("./normalizeFontWeight");

describe('normalizeFontWeight', () => {
  it('should normalize 100', () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('100')).toEqual(100);
  });
  it('should normalize 200', () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('200')).toEqual(200);
  });
  it('should normalize 300', () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('300')).toEqual(300);
  });
  it('should normalize 400', () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('400')).toEqual(400);
  });
  it('should normalize 500', () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('500')).toEqual(500);
  });
  it('should normalize 600', () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('600')).toEqual(600);
  });
  it('should normalize 700', () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('700')).toEqual(700);
  });
  it('should normalize 800', () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('800')).toEqual(800);
  });
  it('should normalize 900', () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('900')).toEqual(900);
  });
  it("should normalize 'bold' to 700", () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('bold')).toEqual(700);
  });
  it("should normalize 'normal' to 400", () => {
    expect((0, _normalizeFontWeight.normalizeFontWeight)('normal')).toEqual(400);
  });
});
//# sourceMappingURL=normalizeFontWeight.spec.js.map